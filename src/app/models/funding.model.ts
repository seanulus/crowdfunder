export class Funding {
  constructor (public name: string, public starter: string, public description: string, public raised: number, public goal: number, public purpose: string, public reward: String[]) { }
}
